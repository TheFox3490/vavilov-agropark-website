/* Картинки, которые фронтенд берёт напрямую. Всё остальное — обложки проектов,
   услуг, новостей и фотографии сотрудников — лежит в базе и правится
   из админки; при первом запуске туда попадают файлы из макета,
   их список в backend/app/cli.py.

   Имена файлов сгенерированы из имён слоёв Figma скриптом
   tools/figma_map_assets.py. Хвост в имени — начало imageRef: дизайнер
   называет разные слои одинаково («Изображение3» на двух экранах Стартапов),
   и без хвоста файлы затирали друг друга.

   Все картинки пережаты в WebP скриптом tools/bake_media.py, исходники
   из макета лежат в figma-export/media-sources и в образ не попадают. */

export const MEDIA = {
  // Первый экран, общий для всех страниц
  hero: "/media/glavnaya--imgbackground-1-8f81ae.webp",

  // Главная → «Мы предлагаем»
  offerCard: "/media/vav--imghawaii-1-a0599a.webp",
};
